{
  "name": "mapping-tfbs",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node app.js",
    "client-install": "npm install --prefix client",
    "client": "npm start --prefix client",
    "install-all": "npm install && npm run client-install",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "nodemon app.js",
    "test": "jest --watchAll",
    "example": "cd client && npm install && npm run build",
    "heroku-postbuild": "rm -rf ./build && react-scripts build && rm -rf ../build && mv ./build ..//build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@koa/cors": "^2.2.3",
    "concurrently": "^5.1.0",
    "koa": "^2.11.0",
    "koa-bodyparser": "^4.2.1",
    "koa-logger": "^3.2.1",
    "koa-router": "^8.0.7",
    "koa-static": "^5.0.0",
    "superagent": "^5.2.2"
  },
  "devDependencies": {
    "jest": "^24.9.0",
    "supertest": "^4.0.2"
  }
}
